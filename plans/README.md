# Implementation Plans

First batch (001–003) generated by the improve skill on 2026-06-11 for
`features/mobile-lite/`. Second batch (004–006) generated on 2026-06-13 — a
direction/quality pass focused on the Mobile Lite frontier and agent-facing docs.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update the
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Close Mobile Lite lifecycle leaks | P1 | M | - | DONE |
| 002 | Roll back Mobile Lite ignore changes when saving fails | P1 | M | 001 | DONE |
| 003 | Clean up the Mobile Lite image crop dialog lifecycle | P2 | M | 001 | DONE |
| 004 | CLAUDE.md reflects the real feature surface; no dead doc links | P1 | S | - | REJECTED — already current on disk; finding came from a stale in-context CLAUDE.md snapshot (see plan) |
| 005 | Spike — AI thread summaries inside Mobile Lite | P2 | M | - | DONE (uncommitted on develop) — spike ported from the main-based worktree to develop by the advisor: tokens de-duped (imported from panel-tokens), verified on develop (compile ✅, 221 mobile-lite tests ✅, lint ✅). Awaiting user commit + Firefox-Android manual smoke. Follow-up: Gemini key field in Settings tab |
| 006 | Spike — draft autosave for the Mobile Lite editor | P2 | M | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Recommended order for the new batch

- **004 first** — S-effort, zero-risk, and it fixes the agent-facing feature map
  that every later task (including 005/006) relies on for context.
- **005 and 006 are independent** spikes; do whichever delivers more value first.
  Recommendation: 005 (AI summaries) for the bigger "wow", 006 (draft autosave)
  for the bigger "never lose work" safety win.

## Dependency notes

- 002 and 003 depend on 001 (earlier batch) because lifecycle cleanup should be
  reliable before adding more async failure handling around user-card actions
  and dialogs.
- 004, 005, 006 have no hard dependencies on each other. 005 and 006 are
  design/spike plans: each produces a `plans/00X-design-notes.md` plus a minimal
  flag-gated prototype, not a finished feature, and should spawn a follow-up
  build plan once its decisions are settled.

## Findings considered and rejected

(2026-06-13 pass — recorded so they aren't re-audited.)

- **Infinite scroll on Mobile Lite (D3)**: deferred, not rejected. Real value,
  but memory/placeholder management on mobile is M–L effort and riskier than the
  two spikes chosen; revisit after 005/006.
- **"Lo mío" panel tab (bookmarks/saved threads on mobile, D4)**: deferred. Good
  adjacent-possible fit for the refactored panel, but lower urgency than AI
  summaries and draft safety; candidate for the next batch.
- **Direct unit tests for the new Mobile Lite hooks / storage card (Q3)**: not
  worth a standalone plan now — the hooks are exercised indirectly by
  `features/mobile-lite/logic/panel.test.tsx`. Fold targeted `use-storage-usage`
  tests into whichever plan next touches that area.

(2026-06-11 pass — retained.)

- Mobile Lite image upload crossing extension boundaries: rejected. `services/api/imgbb.ts`
  is already an RPC facade and `entrypoints/background/upload-handlers.ts` performs
  the external fetches.
- Splitting all of `editor-lite.ts`: deferred. The file is large, but extracting it
  without first tightening crop/upload lifecycle tests would make the refactor riskier.
